import classes from './Card.module.css';

function Card() {
  return (
    <div className={classes.cardContainer}>
        <p className={classes.description}>test</p>
    </div>
  );
}

export default Card;
